server:
  port: 8082
spring:
  application:
    name: ProductService
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZon: http://localhost:8761/eureka
  instance:
    hostname: localhost
