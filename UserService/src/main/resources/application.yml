server:
  port: 8081
spring:
  application:
    name: UserService
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZon: http://localhost:8761/eureka
  instance:
    hostname: localhost
