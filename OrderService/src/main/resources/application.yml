server:
  port: 8083
spring:
  application:
    name: OrderService
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZon: http://localhost:8761/eureka
  instance:
    hostname: localhost
